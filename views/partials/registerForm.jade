.member.clearfix
  - for (var i = 0; i < count; i++)
    h4 成員資料 - #{i + 1}
    - var j = 0
    each regData in registrationData
      .column
        label= regData.name
        if regData.type === 'text' || regData.type === 'number' || regData.type === 'tel' || regData.type === 'email'
          input(type='text', name='attenderField-#{i}[#{j}]')
        else if regData.type === 'date'
          input(type='text', name='attenderField-#{i}[#{j}]', class='date')
        else if regData.type === 'select'
          .select
            select(name='attenderField-#{i}[#{j}]')
              each item in regData.value
                option= item
      - j++